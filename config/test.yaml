application: chatbot
profile: normal
target_host: http://localhost:8000
environment: load_test
header_prefix: X-Test-
workload:
  input_tokens:
    min: 100
    max: 1000
  output_tokens:
    min: 50
    max: 500
